#include "app/cpp/SystemMenus.hpp"

namespace phvikapen::app {

// Only macOS adds items of its own; everywhere else the menus are the ones the application makes.
void keepSystemItemsOutOfMenus() {}

}
